# MyShell v2.12.1

#### 🐛 修复

- **AI 模型选择器即时刷新，新增模型无需重启即可选到**：在设置里给供应商新增模型后，AI 面板的模型选择器一直看不到刚添加的模型，必须重启应用才能选到——AI 面板的供应商/模型列表原先只在面板打开时加载一次，不随设置里的增删改刷新。现改为**每次点开模型选择器都重新读取最新列表**，新增/删除/编辑的模型即时出现、即时可选。顺带修正选中状态显示：新模型追加在列表末尾、并非供应商主模型，而头部标签与下拉高亮原先固定显示主模型，选中新模型后看不出已生效；新增 `get_active_ai_selection` 命令读回具体选中的模型（`active_model_string`），标签与高亮与实际使用的模型保持一致，且跨面板重开/跨重启不丢。
