# 发布暂存 / Release Notes Staging

打包（release）前的工作积攒区。每完成一项具体改动（功能 / 修复 / 优化 / 安全），
追加一行到下方「待发布条目」；`打包` 时**以本文件为主**生成 CHANGELOG 新版本节、
判定版本号，发布后**清空待发布条目**并把 `baseline` 更新为刚发布的版本号。

- `baseline: v1.11.0` —— 上一个已发布版本（`打包` 时作为 `git diff` 完整性校验的起点）

> 写入时机（高优先级，绑 doc-after-feature）：每完成一项改动，写 `progress.md` 阶段
> 日志的**同时**，在本文件「待发布条目」追加一行。一行格式：
> `- <类型emoji> <一句话描述>`（✨新增 / 🛠️优化 / 🐛修复 / 🔒安全）。纯讨论 / 问答不记。

---

## 待发布条目（打包后清空）

<!-- 上一个版本 v1.11.0 已发布。新的改动完成后在此追加一行：`- <emoji> <一句话描述>` -->

- 🛠️优化 升级安装时旧版本自动静默卸载，不再需要手动先卸载再安装（自定义 NSIS 模板，旧版卸载加 /S 静默执行）
- 🐛修复 执行长时间无输出的命令（如 find | sort）时 SSH 连接被误断（用 keepalive 心跳替代 inactivity_timeout 静默超时）
- 🛠️优化 匿名版本统计改为每次版本升级都弹窗询问，不再记住首次同意后静默上报

