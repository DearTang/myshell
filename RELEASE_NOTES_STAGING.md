# 发布暂存 / Release Notes Staging

打包（release）前的工作积攒区。每完成一项具体改动（功能 / 修复 / 优化 / 安全），
追加一行到下方「待发布条目」；`打包` 时**以本文件为主**生成 CHANGELOG 新版本节、
判定版本号，发布后**清空待发布条目**并把 `baseline` 更新为刚发布的版本号。

- `baseline: v1.7.0` —— 上一个已发布版本（`打包` 时作为 `git diff` 完整性校验的起点）

> 写入时机（高优先级，绑 doc-after-feature）：每完成一项改动，写 `progress.md` 阶段
> 日志的**同时**，在本文件「待发布条目」追加一行。一行格式：
> `- <类型emoji> <一句话描述>`（✨新增 / 🛠️优化 / 🐛修复 / 🔒安全）。纯讨论 / 问答不记。

---

## 待发布条目（打包后清空）

<!-- 上一个版本 v1.7.0 已发布。新的改动完成后在此追加一行：`- <emoji> <一句话描述>` -->

- ✨ 连接软删除 + 回收站：删除的连接可在「设置 → 找回连接」恢复，最多保留 30 条
- ✨ 新增「找回连接」窗口，按删除时间倒序排列，支持单条找回、彻底删除和清空回收站
- 🛠️ 连接重命名移入 ⋯ 更多操作菜单，连接右键不再弹菜单
- 🛠️ 删除连接和文件夹增加二次确认弹窗（自定义弹窗替代系统确认框，修复部分环境不弹出的问题）
- 🛠️ 删除文件夹时其下的连接一并进入回收站（可恢复），不再移到根目录
- 🐛 修复大尺寸背景图设置后不生效的问题（选图后自动压缩到 1920px / JPEG，避免超出本地存储配额）
- 🐛 修复部分用户看不到终端光标和选区的问题（强制光标使用高对比前景色、选区提升不透明度）
