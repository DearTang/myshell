# 发布暂存 / Release Notes Staging

打包（release）前的工作积攒区。每完成一项具体改动（功能 / 修复 / 优化 / 安全），
追加一行到下方「待发布条目」；`打包` 时**以本文件为主**生成 CHANGELOG 新版本节、
判定版本号，发布后**清空待发布条目**并把 `baseline` 更新为刚发布的版本号。

- `baseline: v2.12.2` —— 上一个已发布版本（`打包` 时作为 `git diff` 完整性校验的起点）

> 写入时机（高优先级，绑 doc-after-feature）：每完成一项改动，写 `progress.md` 阶段
> 日志的**同时**，在本文件「待发布条目」追加一行。一行格式：
> `- <类型emoji> <一句话描述>`（✨新增 / 🛠️优化 / 🐛修复 / 🔒安全）。纯讨论 / 问答不记。

---

## 待发布条目（打包后清空）

<!-- 上一个版本 v2.12.2 已发布。新的改动完成后在此追加一行：`- <emoji> <一句话描述>` -->
- 🐛修复 sz 下载两类卡死：远端文件无读权限或目标是目录时，接收器无法识别 lrzsz 中止序列（CAN 连发/ZABORT），会话永不结束、终端输出被吞；现立即恢复终端并显示失败原因，另加 30s 空闲超时兜底
- ✨新增 SFTP 文件夹递归下载（空目录保留、符号链接循环保护）+ 多文件并发下载（设置 → 文件传输，默认 3 线程，1–16 可调，复用单条 SSH 连接）
- ✨新增 ZMODEM 文件夹递归传输（双向）：下载 `sz -r 目录` 与上传选文件夹（GUI rz 弹文件/文件夹选择条、MCP zmodem_upload 支持目录），ZFILE offer 携带相对路径、远端 rz 自动重建目录树，路径段清洗防 `..`/盘符逃逸；ZMODEM 协议单流串行、无并发（设置页已注明）
