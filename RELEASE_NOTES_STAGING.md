# 发布暂存 / Release Notes Staging

打包（release）前的工作积攒区。每完成一项具体改动（功能 / 修复 / 优化 / 安全），
追加一行到下方「待发布条目」；`打包` 时**以本文件为主**生成 CHANGELOG 新版本节、
判定版本号，发布后**清空待发布条目**并把 `baseline` 更新为刚发布的版本号。

- `baseline: v1.6.1` —— 上一个已发布版本（`打包` 时作为 `git diff` 完整性校验的起点）

> 写入时机（高优先级，绑 doc-after-feature）：每完成一项改动，写 `progress.md` 阶段
> 日志的**同时**，在本文件「待发布条目」追加一行。一行格式：
> `- <类型emoji> <一句话描述>`（✨新增 / 🛠️优化 / 🐛修复 / 🔒安全）。纯讨论 / 问答不记。

---

## 待发布条目（打包后清空）

- 🐛 修复「安装并重启」按钮报 os error 740（请求的操作需要提升）—— 改用 ShellExecuteW+runas 触发 UAC，非管理员用户也能正常升级
- 🛠️ 更新弹窗简化为单层卡片（去掉双层玻璃外壳），按钮改为「忽略」和「更新」
- 🐛 修复更新弹窗版本号显示 "vv1.6.1" 双 v 问题（tag 已含 v 前缀时不再重复添加）
- 🛠️ 关于对话框「去下载」改为「更新」—— 点击自动下载安装（进度条+安装并重启），失败才回退浏览器下载；同时保留「网页下载」按钮作为备选
- 🐛 修复进度条点击更新时先跳到中间再从 0 开始的问题（pct 为 null 时 fallback 从 30% 改为 0%）
