# 发布暂存 / Release Notes Staging

打包（release）前的工作积攒区。每完成一项具体改动（功能 / 修复 / 优化 / 安全），
追加一行到下方「待发布条目」；`打包` 时**以本文件为主**生成 CHANGELOG 新版本节、
判定版本号，发布后**清空待发布条目**并把 `baseline` 更新为刚发布的版本号。

- `baseline: v1.6.2` —— 上一个已发布版本（`打包` 时作为 `git diff` 完整性校验的起点）

> 写入时机（高优先级，绑 doc-after-feature）：每完成一项改动，写 `progress.md` 阶段
> 日志的**同时**，在本文件「待发布条目」追加一行。一行格式：
> `- <类型emoji> <一句话描述>`（✨新增 / 🛠️优化 / 🐛修复 / 🔒安全）。纯讨论 / 问答不记。

---

## 待发布条目（打包后清空）

- 🛠️ AI 面板自动附带的终端输出限制为 5000 字符，避免超长输出撑爆上下文
- 🛠️ AI 面板用户引用内容默认单行显示，双击可展开查看完整内容
- 🛠️ 新建/编辑连接弹窗增加必填字段红色标注（*），保存时自动聚焦首个未填项并抖动提示
- 🛠️ 快捷命令编辑器增加名称和命令必填标注，未填写时禁用保存按钮
- 🛠️ 自定义主题色增加 HEX 颜色值校验，非法输入时禁用保存并提示
- 🛠️ 连接名称自动从主机地址填充的逻辑优化：用户手动修改后不再被主机输入覆盖

