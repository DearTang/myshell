# 发布暂存 / Release Notes Staging

打包（release）前的工作积攒区。每完成一项具体改动（功能 / 修复 / 优化 / 安全），
追加一行到下方「待发布条目」；`打包` 时**以本文件为主**生成 CHANGELOG 新版本节、
判定版本号，发布后**清空待发布条目**并把 `baseline` 更新为刚发布的版本号。

- `baseline: v1.10.2` —— 上一个已发布版本（`打包` 时作为 `git diff` 完整性校验的起点）

> 写入时机（高优先级，绑 doc-after-feature）：每完成一项改动，写 `progress.md` 阶段
> 日志的**同时**，在本文件「待发布条目」追加一行。一行格式：
> `- <类型emoji> <一句话描述>`（✨新增 / 🛠️优化 / 🐛修复 / 🔒安全）。纯讨论 / 问答不记。

---

## 待发布条目（打包后清空）

<!-- 上一个版本 v1.10.2 已发布。新的改动完成后在此追加一行：`- <emoji> <一句话描述>` -->

- ✨新增 AI 设置界面左右布局：左侧供应商列表（新建/选中/双击重命名/启用禁用开关），右侧详情编辑（预设模板下拉选择/基本信息/模型管理）
- ✨新增 每个供应商支持多模型管理：从接口自动获取可用模型（勾选添加）、手动添加模型、主模型标记
- ✨新增 AI 聊天窗口模型选择器改为两步选择（先选供应商再选模型），仅展示已启用的自定义供应商
- 🛠️优化 新建供应商需添加模型并测试通过后才可创建，避免无效配置
- 🛠️优化 测试/保存/删除等操作反馈改为应用内 Toast 浮层提示（顶部居中）
- 🛠️优化 设置窗口改为固定高度，切换类别时窗口大小不再跳变
- 🛠️优化 测试功能改为针对用户正在编辑的供应商而非全局活跃供应商
- 🐛修复 保存后获取模型列表提示 key 无效的问题（新增服务端解密 key 的获取命令）
- 🐛修复 dialog.message 权限未授权导致 alert 弹窗失效的问题