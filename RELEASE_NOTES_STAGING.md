# 发布暂存 / Release Notes Staging

打包（release）前的工作积攒区。每完成一项具体改动（功能 / 修复 / 优化 / 安全），
追加一行到下方「待发布条目」；`打包` 时**以本文件为主**生成 CHANGELOG 新版本节、
判定版本号，发布后**清空待发布条目**并把 `baseline` 更新为刚发布的版本号。

- `baseline: v1.8.0` —— 上一个已发布版本（`打包` 时作为 `git diff` 完整性校验的起点）

> 写入时机（高优先级，绑 doc-after-feature）：每完成一项改动，写 `progress.md` 阶段
> 日志的**同时**，在本文件「待发布条目」追加一行。一行格式：
> `- <类型emoji> <一句话描述>`（✨新增 / 🛠️优化 / 🐛修复 / 🔒安全）。纯讨论 / 问答不记。

---

## 待发布条目（打包后清空）

<!-- 上一个版本 v1.8.0 已发布。新的改动完成后在此追加一行：`- <emoji> <一句话描述>` -->
- ✨新增 设置面板左右分栏导航，按「外观 / AI / 安全 / 数据管理 / 快捷命令」分类组织
- ✨新增 终端渲染后端可选（自动 / Canvas / WebGL / DOM），解决部分用户光标和选区不可见的问题
- ✨新增 全局日志监控：前端崩溃 / 未捕获异常自动转发到后台日志，便于异常定位
- 🛠️优化 终端默认改用 Canvas 渲染器 + 条形光标（cursorStyle: bar），光标更稳定可见且不丢失闪烁
- 🛠️优化 后端关键路径补全日志（SSH/SFTP/本地终端/数据库/备份/AI/代理），会话生命周期完整可追溯
- 🛠️优化 设置新增「禁用 GPU 硬件加速」开关，作为 GPU 合成异常时的最终兜底（重启生效）
- 🐛修复 修复 forceVisibleCursor 注释声明 cursorStyle:"bar" 但代码从未设置的遗留缺陷

