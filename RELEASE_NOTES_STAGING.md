# 发布暂存 / Release Notes Staging

打包（release）前的工作积攒区。每完成一项具体改动（功能 / 修复 / 优化 / 安全），
追加一行到下方「待发布条目」；`打包` 时**以本文件为主**生成 CHANGELOG 新版本节、
判定版本号，发布后**清空待发布条目**并把 `baseline` 更新为刚发布的版本号。

- `baseline: v2.12.1` —— 上一个已发布版本（`打包` 时作为 `git diff` 完整性校验的起点）

> 写入时机（高优先级，绑 doc-after-feature）：每完成一项改动，写 `progress.md` 阶段
> 日志的**同时**，在本文件「待发布条目」追加一行。一行格式：
> `- <类型emoji> <一句话描述>`（✨新增 / 🛠️优化 / 🐛修复 / 🔒安全）。纯讨论 / 问答不记。

---

## 待发布条目（打包后清空）

- 🔒安全 GUI↔MCP localhost IPC 桥增加随机会话令牌认证：每条命令须携带端口文件中的令牌，否则在分发前拒绝（防本机其他用户进程窃取解密凭证，含单实例 shutdown 路径，兼容旧格式端口文件）
- 🔒安全 russh 0.50→0.60.3（加密后端换 ring，免 NASM/CMake）：修复 RUSTSEC-2026-0154/0153 两个 high 漏洞（恶意 SSH 服务器可触发无上限内存分配导致客户端拒绝服务）
- 🛠️优化 依赖全量更新：npm 漏洞清零（nanoid/postcss 两个 high）+ tauri 前端套件升至最新补丁版；Rust 侧 cargo update 160+ 包（tauri 2.11.5、rustls 0.23.43、russh-sftp 2.4.0 等）
- 🛠️优化 MCP 高危操作被用户拒绝后返回「硬性停止」指令：AI 必须停止一切后续操作、输出当前任务说明，等待用户确认是否继续（覆盖全部确认弹窗出口与异步任务）
