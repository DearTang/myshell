# 发布暂存 / Release Notes Staging

打包（release）前的工作积攒区。每完成一项具体改动（功能 / 修复 / 优化 / 安全），
追加一行到下方「待发布条目」；`打包` 时**以本文件为主**生成 CHANGELOG 新版本节、
判定版本号，发布后**清空待发布条目**并把 `baseline` 更新为刚发布的版本号。

- `baseline: v1.6.3` —— 上一个已发布版本（`打包` 时作为 `git diff` 完整性校验的起点）

> 写入时机（高优先级，绑 doc-after-feature）：每完成一项改动，写 `progress.md` 阶段
> 日志的**同时**，在本文件「待发布条目」追加一行。一行格式：
> `- <类型emoji> <一句话描述>`（✨新增 / 🛠️优化 / 🐛修复 / 🔒安全）。纯讨论 / 问答不记。

---

## 待发布条目（打包后清空）

<!-- 上一个版本 v1.6.3 已发布。新的改动完成后在此追加一行：`- <emoji> <一句话描述>` -->

- ✨ 侧栏支持拖拽调整宽度（宽度持久化），连接名悬停可查看全称及主机地址
- ✨ 新增「当前会话」与「广播」下拉面板，可查看全部标签页全称、快速切换与关闭
- ✨ 下拉面板支持一键清理所有掉线会话
- ✨ AI 助手选区预览默认单行展示，双击展开完整内容
- ✨ 广播组掉线重连支持级联：点击重连一个，同广播组其他掉线会话自动一起重连
- 🛠️ 广播同连接重复加入时提示确认，支持「本次会话不再提醒」（重启重置，记住上次勾选偏好）
- 🛠️ 连接对话框必填项标注红色星号，校验不通过时对应字段红框抖动并精确提示缺失项
- 🛠️ 自定义主题颜色输入增加 hex 格式校验，快捷命令必填项标注星号
- 🐛 修复广播组成员重连后因 sessionId 变更而脱组的问题
- 🐛 修复连接名称跟随主机地址输入时首个字符后停止同步的问题
- 🐛 AI 助手自动附带的最近输出内容限制为 5K 字符，选区内容不受限
