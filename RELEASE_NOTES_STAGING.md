# 发布暂存 / Release Notes Staging

打包（release）前的工作积攒区。每完成一项具体改动（功能 / 修复 / 优化 / 安全），
追加一行到下方「待发布条目」；`打包` 时**以本文件为主**生成 CHANGELOG 新版本节、
判定版本号，发布后**清空待发布条目**并把 `baseline` 更新为刚发布的版本号。

- `baseline: v1.10.1` —— 上一个已发布版本（`打包` 时作为 `git diff` 完整性校验的起点）

> 写入时机（高优先级，绑 doc-after-feature）：每完成一项改动，写 `progress.md` 阶段
> 日志的**同时**，在本文件「待发布条目」追加一行。一行格式：
> `- <类型emoji> <一句话描述>`（✨新增 / 🛠️优化 / 🐛修复 / 🔒安全）。纯讨论 / 问答不记。

---

## 待发布条目（打包后清空）

<!-- 上一个版本 v1.10.1 已发布。新的改动完成后在此追加一行：`- <emoji> <一句话描述>` -->
- 🛠️优化 反馈提交改为唤起本地邮件客户端（mailto），内容自动复制到剪贴板，弃用不稳定的 Web3Forms/第三方图床
- 🛠️优化 反馈窗口底部新增 Gitee Issue 提交入口
- 🛠️优化 关闭反馈窗口时自动清空本地反馈包目录，防止堆积
- 🐛修复 「打开反馈包所在文件夹」按钮点击无效（reveal_path 白名单限制）
- 🐛修复 反馈提交失败时界面不明显，改为红色 ❌ 醒目提示
- 🛠️优化 资源管理器窗口去重：已打开同名目录时激活已有窗口而非重复打开

