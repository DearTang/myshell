{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main window. Grants event subscription permission so the frontend can receive ssh_output/ssh_closed emits, plus dialog plugin access for ZMODEM file pickers.",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "core:window:allow-maximize",
    "core:window:allow-unmaximize",
    "core:window:allow-is-maximized",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "dialog:allow-confirm",
    "dialog:allow-ask"
  ]
}
