## v2.13.0（2026-09-02）

#### ✨ 新增

- **SFTP 文件夹递归下载 + 多文件并发下载**：SFTP 面板勾选文件夹（可与文件混选）即可递归下载整个子树——本地按 `<目标目录>/<文件夹名>/...` 镜像目录结构，空目录保留，符号链接循环有深度保护（64 层上限转为单条报错而非卡死）。多文件同时传输时按可配置线程数并行拉取：设置 → 文件传输，默认 3 线程、1–16 可调，改动对下一次下载即时生效；所有线程复用同一条 SSH 连接（russh-sftp 按请求 ID 多路复用，协议合法、无每线程握手开销），海量小文件提速明显。单文件下载、上传、取消行为完全不变。
- **ZMODEM 文件夹递归传输（双向）**：下载方向，终端 `sz -r 目录` 与 MCP `zmodem_download` 传目录均可递归拉回整个子树——接收端按 ZFILE offer 携带的相对路径自动创建目录层级，路径逐段清洗（`..`、绝对路径前缀、盘符全部丢弃，`:` 映射为 `_` 同时中和 NTFS ADS），子树永远落在所选保存目录内。上传方向，rz 支持选择文件夹递归上传：终端敲 `rz` 后弹出「选择文件 / 选择文件夹」选择条（OS 对话框无法一次混合选择两类），本地目录树展开后按相对路径 offer，远端 lrzsz rz 自动重建目录树；MCP `zmodem_upload` 的 `local_path` 同样支持目录。只选空文件夹时立即结束并提示，不让 rz 干等超时。ZMODEM 为协议单流串行——多文件顺序传输、无并发，不适用 SFTP 的并发设置（设置页已注明）。

#### 🐛 修复

- **sz 下载两类卡死：远端文件无读权限或目标是目录时应用假死**：`sz` 先发握手帧之后才逐个打开文件，失败时 lrzsz 打印错误并连发 10×CAN 中止序列退出，而原生 ZMODEM 接收器不识别该序列——会话永远无法结束，此后 shell 的全部输出（提示符、按键回显）被当作协议数据吞掉，终端完全冻结（输入也无回显）。现接收器自动识别 CAN 连发（≥5 个连续原始 0x18，跨网络包到达也能命中）与 ZABORT/ZFERR 帧，立即结束会话、终端恢复可用并以红色 `[ZMODEM 错误]` 显示原因；另加 30 秒空闲超时兜底，覆盖 sz 被信号杀死等静默退出场景（用户开着保存目录选择框时豁免，不误杀）。前端补上 `zmodem_error` 事件订阅——此前 Rust 已发出该事件但无人消费，失败原因全部丢失。顺带修复 `command_rules` 模块单元测试的既有编译错误（结构体新增字段后测试未同步，导致 `cargo test` 全量编译失败）。
